export declare class Size {
    private prep;
    private _width;
    get width(): number;
    set width(v: number);
    private _height;
    get height(): number;
    set height(v: number);
    toString(): string;
    constructor(width: number, height: number);
}
